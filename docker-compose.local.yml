services:
  api:
    ports:
      - "8000:8000"
    environment:
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-local-admin}
      SEED_LOCAL: ${SEED_LOCAL:-true}
      FRONTEND_ORIGIN: ${FRONTEND_ORIGIN:-http://localhost:5173}

  frontend:
    ports:
      - "5173:3000"
